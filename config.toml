# Replace all textile links with markdown links
# %s/"\([^"]\+\)":\([^ ]\+\)/[\1](\2)/g
# Thumbnails:
# for f in *.png; do convert -size 440x880 "$f" -resize 440x880 +profile '*' \
# $(basename $f .png)-thumb.png; done
languageCode = "pl"
title = "Automaciej"
baseurl = "http://blizin.ski"
paginate = 10
theme = "theme005"
[params]
  twitterName = "automaciej"
  githubName = "automatthias"
  googleplus = "MaciejBliziński"
  description = "Dwa razy dwa jest pięć, więc myj zęby! Strona domowa automacieja."
[taxonomies]
  tag = "tags"
[permalinks]
  post = "/:year/:month/:filename/"
  page = "/:filename/"
[blackfriday]
  # W przypisach mamy rzeczy typu "2010/4" które są formami dat.
  fractions = false
